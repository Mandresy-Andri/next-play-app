# Copy this file to .env.local and fill in the values.
# .env.local is gitignored — never commit real keys.

# --- Supabase project (frontend, safe to ship to browser) ---
VITE_SUPABASE_URL=https://zgrxevscvjyblzfddsbb.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Note: The Supabase MCP server used by Claude Code authenticates via
# browser-based OAuth against the hosted remote MCP at
# https://mcp.supabase.com/mcp — no Personal Access Token or env var is needed.
# See .mcp.json for the server configuration.
